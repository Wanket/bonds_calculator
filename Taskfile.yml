version: "3"
tasks:
  dependencies:
    cmds:
      - go mod download
  generate:
    cmds:
      - go generate ./...
  prepare:
    cmds:
      - task: dependencies
      - task: generate
  build:
    cmds:
      - go build -o ./out/bc ./cmd/main.go
  test:
    cmds:
      - go test -v ./...
  run:
    cmds:
      - go run ./...
